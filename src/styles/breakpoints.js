const breakpoints = {
  mobile: '300px', //모바일 최소 크기
  tablet: '768px', //태블릿 최소 크기
  pc: '900px',
  maxblock: '1200px' //좌측, 우측 배치일 때 최소 크기 -> 이보다 작을 경우 우측 컨테이너가 좌측 컨테이너 아래로 이동함
}

export default breakpoints;
